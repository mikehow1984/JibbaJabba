<div class='OG-post-container'><div id='topic-title'><%= @topic.title =%></div>
<div><%= @topic.content %></div></div>
<div>Posted: <%= format_time(@topic.unix_time, @topic.coord_lat, @topic.coord_long) %></div>
<% for post in @topic.posts %>
	<div class='post-container'>
		<div><%= post.content %></div>
		<div>Posted: <%= format_time(post.unix_time, post.coord_lat, post.coord_long) %></div>
	</div>
<% end %>

